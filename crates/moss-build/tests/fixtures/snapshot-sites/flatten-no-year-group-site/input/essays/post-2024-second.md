---
date: 2024-09-01
---
# Newer post

A second post, also from 2024 — same year as the sibling so `spans_multiple_years` returns false and the auto-detected `children_group` is "none". Pairs with `flatten-year-group-site` to lock the conditional: same flatten path (`children_depth: all`), different year-span signal, different outcome.
